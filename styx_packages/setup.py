from setuptools import setup, find_packages

setup(
    name="styx_logger",
    version="0.1.0",
    packages=find_packages(),
    description="Shared logging package for Styx project",
    install_requires=[
        # Any dependencies
    ],
)
